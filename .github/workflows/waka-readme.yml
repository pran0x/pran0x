name: Waka Readme

on:
  # for manual workflow trigger
  workflow_dispatch:
  schedule:
    # runs at 12 AM UTC (5:30 AM IST)
    - cron: "0 0 * * *"

jobs:
  update-readme:
    name: WakaReadme DevMetrics
    runs-on: ubuntu-latest
    steps:
      - uses: Pranx/waka-readme@master # this action name
        with:
          WAKATIME_API_KEY: ${{ waka_86e0ede3-1ce7-4c9b-a673-ca8a2e2f1796}}
